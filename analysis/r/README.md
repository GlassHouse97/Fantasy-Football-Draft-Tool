# Optional R/Quarto companion

R and Quarto are not installed in the current environment, so they do not block the Python build. Later companion analyses will use Arrow, DuckDB, dplyr, ggplot2, tidymodels, broom, rsample, and yardstick to independently inspect model assumptions and evaluation.
